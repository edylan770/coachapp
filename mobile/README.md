# TrainerOS client app (mobile)

Placeholder. The Expo (React Native) client app is built in **Phase 2** per
`PROJECT_SPEC.md` §8: offline-first workout logging with a local SQLite sync
queue, and check-in submission.

Whether this joins the npm workspace or stays a standalone package will be
decided when it's scaffolded (Metro bundler has known quirks with hoisted
workspace deps).
